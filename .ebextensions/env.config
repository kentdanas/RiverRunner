files:
  "/etc/httpd/conf.d/wsgi_custom.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      WSGIApplicationGroup %{GLOBAL}

commands:
  make_dash_dir:
    command: "mkdir -p /home/wsgi && chown -R wsgi:wsgi /home/wsgi"